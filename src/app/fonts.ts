// Font configuration
// Fonts are loaded via @import in globals.css (no build-time Google Fonts fetch).
// Special Elite is used as the primary UI font for labels, toggles, and meta text.

export const fontVariables = "--font-sans --font-serif --font-special-elite";

// Lightweight class strings for the <html> tag
export const fontClassNames = "";
